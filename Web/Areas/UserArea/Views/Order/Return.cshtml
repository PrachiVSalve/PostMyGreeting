  @model Return
<div class="col-lg-12">

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Order Return</h5>
            <h5> Order No:@TempData["OrderId"] </h5>
            <div>
                @if (ViewBag.Message != null)
                {
                    <div class="alert alert-info">@ViewBag.Message</div>
                }

            </div>
            <!-- Vertical Form -->
            <form class="row g-3" asp-action="RaiseComplaint" method="Post">
                <input type="hidden" asp-for="OrderId" value="@TempData["OrderId"] " />

                <input type="hidden" asp-for="ProductId" id="productid" />


                <div class="col-6">
                    @await Component.InvokeAsync("GiftComplaintVC", new { orderid = @TempData["OrderId"] })
                </div>



                <div class="col-6">
                    @await Component.InvokeAsync("GreetingComplaintVC", new { orderid = @TempData["OrderId"] })
                </div>



                <div class="col-6">
                    <label class="form-label">Return Reason</label>
                    <input asp-for="ReturnReasion" class="form-control" />
                    <span asp-validation-for="ReturnReasion" class="text-danger"></span>
                </div>

                <div class="col-6">
                    <label class="form-label">Return Date</label>
                    <input asp-for="ReturnDate" class="form-control" />

                    <span asp-validation-for="ReturnDate" class="text-danger"></span>
                </div>



                <div class="text-center">
                    <button type="submit" class="btn btn-primary" asp-route-id="@TempData["OrderId"]">Save</button>
                </div>
            </form><!-- Vertical Form -->

        </div>
    </div>


</div>
<script type="text/javascript">
    $(function () {
        $("#gift").change(function () {
            if (this.checked) {
                var a = $("#giftid").val();
                $("#productid").val(a);
            }

        });


        $("#greeting").change(function () {
            if (this.checked) {
                var a = $("#greetingid").val();
                $("#productid").val(a);
            }

        });


    });
</script>
