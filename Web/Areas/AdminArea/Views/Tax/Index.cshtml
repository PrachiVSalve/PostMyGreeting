@model IEnumerable<Tax>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Tax table</h4>
            
            <div class="table-responsive pt-3">
                <a class="btn btn-success" asp-action="Create"> Create </a>
                <hr />
                <table class="table table-bordered">
                    <thead>
                        <tr>
                           <th>#</th>
                           <th>CGSTRate </th>
                           <th>SGSTRate </th>
                           <th>IGSTRate</th>
                           <th>HSNSACNo</th>
                             
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int srno = 1;
                        }
                        @foreach(var temp in Model)
                        {
                            <tr>
                                <td>@srno</td>
                                <td>@temp.CGSTRate</td>
                                <td>@temp.SGSTRate</td>
                                <td>@temp.IGSTRate</td>
                                <td>@temp.HSNSACNo</td>
                                <td>
                                    <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@temp.TaxId">Edit</a>
                                    |
                                    <a class="btn btn-outline-primary" asp-action="Delete" asp-route-id="@temp.TaxId ">Delete</a>
                                </td>
                            </tr>
                            srno++;
                        }
                        
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>