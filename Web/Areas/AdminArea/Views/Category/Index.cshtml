@model IEnumerable<Category>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Category table</h4>
            
            <div class="table-responsive pt-3">
                <a class="btn btn-success" asp-action="Create"> Create</a>
                <hr />
                <table class="table table-bordered">
                    <thead>
                        <tr>
                           <th>#</th>
                           <th>Category Name</th>
                           
                             
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int srno = 1;
                        }
                        @foreach(var temp in Model)
                        {
                            <tr>
                                <td>@srno</td>
                                <td>@temp.CategoryName</td>
                               
                                <td>
                                    <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@temp.CategoryID">Edit</a>
                                    |
                                    <a class="btn btn-outline-primary" asp-action="Delete" asp-route-id="@temp.CategoryID">Delete</a>
                                </td>
                            </tr>
                            srno++;
                        }
                        
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>